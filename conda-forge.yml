channels:
  sources: [cryoem, defaults, conda-forge]
  targets:
    - [cryoem, main]

github:
  user_or_org: cryoem
  repo_name: eman-feedstock

provider:
  linux: circle

azure:
  build_id: blank
  osx: github_actions
  win: github_actions
